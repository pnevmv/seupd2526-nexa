services:
  bert-api:
    build:
      context: ./code/src/main/python
      dockerfile: Dockerfile
    container_name: bert-server
    ports:
      - "8080:8080"
    volumes:
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  hf_cache: